#ifndef START_END_H
# define START_END_H

#include "typedef.h"

/* @brief Free all the mlx then exit the program.
 * @param tetris the structure storing all pointers of the mlx
 * @return useless since it will exit
 */
int		close_tetris(t_tetris *tetris);

#endif
